# نشر نسب ود الجترة على Render

1. أنشئ مستودع GitHub خاصًا باسم nasab-wad-aljatra.
2. ارفع محتويات المشروع (لا ترفع ZIP نفسه).
3. في Render اختر New → Blueprint ثم اربط المستودع.
4. اختر render.yaml.
5. Render سيبني المشروع ويشغّل npm start.
6. القرص الدائم mounted على /var/data لحفظ قاعدة البيانات والوثائق الخاصة.
7. بعد النشر اختبر /healthz.
8. اربط مزود SMS الحقيقي داخل Environment Variables قبل استخدام OTP مع الجمهور.
9. لا ترفع وثائق هوية حقيقية قبل اختبار الأمان والصلاحيات.

مهم: لا تضع أسرار SMS أو كلمات مرور أو ملفات الهوية داخل GitHub.
